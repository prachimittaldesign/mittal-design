// Shared style for the floating square map-control buttons (view modes,
// recenter, zoom, layers, mobile music toggle) — one size, radius, and glass
// treatment so the whole bottom/side HUD cluster reads as one family.
export const HUD_ICON_BTN =
  'hud hud-hover flex h-[44px] w-[44px] items-center justify-center rounded-[12px] border shadow-[0_3px_14px_rgba(0,0,0,0.14)] backdrop-blur-md transition-colors'
